# Pre-Launch Checklist — Rasid

Generated by Phase 14 (Pre-Launch Release Readiness & Production Validation).
Status as of **2026-08-25**. This is a living document — update status honestly
as gates close; never mark something PASS without real evidence (see
`docs/RELEASE_GATES.md` for the fuller narrative behind each open gate).

Legend: ✅ PASS · ⚠️ PARTIAL/RISK · ⛔ BLOCKER · 👤 EXTERNAL/HUMAN ACTION

## Infrastructure

| Check | Owner | Status | Evidence | Blocking? |
|---|---|---|---|---|
| Vercel frontend deployed and reachable | System | ✅ | `https://rasid-web.vercel.app` live-checked, commit `6510d70` | No |
| Railway API deployed and reachable | System | ✅ | `/health` → `{"status":"ok"}` live-checked | No |
| Railway Worker deployed and running | System | ✅ | Confirmed via code audit — fail-fast startup, dedicated role; no HTTP surface to smoke-test directly | No |
| API health/readiness endpoints exist | System | ⚠️ | `/health` and `/ready` both return the same trivial `{status:"ok"}` — neither checks DB reachability | No (soft launch) — recommend before broad scale |
| Worker health/liveness surface | System | ⚠️ | None exists — orchestrator can only detect a dead process, not a stuck one | No (soft launch) |
| CORS allowlist configured per environment | 👤 Human (Railway) | ⚠️ | Code fails closed to `localhost:3001` if `CORS_ALLOWED_ORIGINS` unset — confirm it's actually set to the Vercel domain in Railway | Confirm, not proven from this session |
| Swagger/API docs exposure decision | 👤 Human | ⚠️ | `/api/v1/docs` always mounted, no env gate | Non-blocking, decide before public launch |
| Sentry/error tracking wired | 👤 Human | ⚠️ | `SENTRY_DSN` declared but nothing initializes a real SDK — pure no-op stubs today | Non-blocking for soft launch, recommend before scale |
| Domain: still on `rasid-web.vercel.app` | 👤 Human | ⚠️ | No custom domain — acceptable for soft launch, decide before broad public marketing | No (soft launch) |

## Database

| Check | Owner | Status | Evidence | Blocking? |
|---|---|---|---|---|
| DB roles least-privilege (`app_runtime`/`app_worker`/`app_platform_admin`) | System | ✅ | Verified live: all 3 roles exist, `NOBYPASSRLS`, correct connection limits | No |
| RLS coverage on all tenant tables | System | ✅ | Verified via migrations: students/groups/sessions/finance/subscriptions all workspace-scoped; platform_admin has a distinct, narrow, read-only cross-tenant policy | No |
| Connection budget headroom | System | ⛔ | **Live-verified real numbers**: `max_connections=60`; `app_runtime` connlimit 20 + `app_worker` 5 + `app_platform_admin` 5 = 30 app budget, plus ~9-14 baseline Supabase infra connections always present. The project's own `docs/RELEASE_GATES.md` (written before this session) claims a 15-connection Session Pooler cap for this tier — this session's live check saw `max_connections=60` at the direct-Postgres level, which is a **different number than what RELEASE_GATES.md assumes**; this discrepancy itself needs a human to resolve by checking the actual Supabase plan/pooler config in the dashboard, not guessed at | **Yes — release gate, needs human verification of actual current tier/pooler config** |
| Backup exists (Supabase automatic) | 👤 Human (Supabase dashboard) | 👤 | Not verifiable from this session — requires Supabase dashboard access to confirm plan tier and automatic-backup/PITR status | Yes, until confirmed |
| Restore drill executed | 👤 Human | ⛔ | **Not proven.** This session performed a real, live logical export of all 14 critical tables from production (row counts match live exactly) — proves extraction works. Restore into a second database was **not** performed (no staging/second Supabase project available in this session). See Part 4 of the final report for exact human steps. | **Yes — release gate** |
| Seed/scale scripts guarded against accidental prod use | System | ⚠️ | No `NODE_ENV`/known-URL guard exists on `seed-scale-dataset.ts`/`cleanup-scale-dataset.ts` — safety currently depends entirely on the operator not pointing `MIGRATION_DATABASE_URL` at prod by mistake. This session used them against prod deliberately and cleaned up fully (verified row counts return to baseline: 16 workspaces/16 users/10 memberships/2 students/3 groups/18 sessions) | Non-blocking, recommend adding a guard |

## Billing

| Check | Owner | Status | Evidence | Blocking? |
|---|---|---|---|---|
| Paddle webhook signature verification | System | ✅ | Real HMAC-SHA256 verify, timing-safe compare, 300s replay window | No |
| Webhook idempotency | System | ✅ | Event-id-keyed idempotency records checked before every state transition | No |
| Pricing page ↔ actual billing truth | 👤 Human decision | ⛔ | **Confirmed real gap, self-documented in code**: only ONE `PADDLE_PRICE_ID` exists; all 6 displayed pricing tiers route to the identical trial/checkout flow with no tier-to-price mapping. `pricing-config.ts`'s own top comment says this explicitly. | **Yes — cannot accept real payment for a specific tier today** |
| Paddle sandbox live-tested this session | 👤 Human (needs credentials) | 👤 | Local `.env` has `PADDLE_API_KEY`/`PADDLE_WEBHOOK_SECRET`/`PADDLE_ENVIRONMENT` all **empty** — this session had no usable Paddle credentials to run a live sandbox test | **Yes — needs a human with real Paddle sandbox credentials** |
| Trial logic (14 days, one per owner) | System | ✅ | DB-level unique constraint on `owner_trial_grants` (email_hash + first_user_id), verified in code | No |
| Entitlement blocking on EXPIRED/PAYMENT_FAILED | System | ✅ | `EntitlementGuard` verified — blocks CORE_OPERATIONS/CREATE_MONTH/TEAM_MANAGEMENT/REPORT_EXPORT; billing routes themselves stay open | No |

## Auth / Email

| Check | Owner | Status | Evidence | Blocking? |
|---|---|---|---|---|
| Signup/login/reset/OTP all wired | System | ✅ | Confirmed via code — all go directly to Supabase Auth client SDK | No |
| Auth email delivery provider | 👤 Human (Supabase dashboard) | 👤 | Delivery relies on Supabase's own default/dashboard-configured SMTP — not present in this repo, not verifiable from code. `RESEND_API_KEY` is declared but unused by any code path (architecture decision not yet implemented) | Confirm actual dashboard SMTP config before relying on volume |
| Rate limiting / brute-force protection on auth | 👤 Human (Supabase dashboard) | ⚠️ P1 | Auth calls bypass this repo's `ThrottlerGuard` entirely (they go straight from browser to Supabase) — protection is 100% dependent on Supabase project-level Auth rate limits, unverifiable from code | Confirm in Supabase dashboard before real users |

## Security

| Check | Owner | Status | Evidence | Blocking? |
|---|---|---|---|---|
| Auth guard coverage | System | ✅ | Verified on all 24 non-exempt controllers | No |
| Workspace/tenant isolation | System | ✅ | Verified at both application-query and RLS layers | No |
| Platform admin isolation | System | ✅ | Gated by DB-table lookup on JWT-verified user id, not a forgeable claim | No |
| service_role key never reaches browser | System | ✅ | Verified — only referenced in server-only files/comments | No |
| Secrets never logged | System | ✅ | Enforced redaction layer on all structured logs | No |
| P0 findings | System | ✅ | **Zero found** | No |
| P1 findings | System | ⚠️ | One — Auth rate-limiting delegated entirely to Supabase (see Auth/Email above) | Confirm before real users |

## Legal

| Check | Owner | Status | Evidence | Blocking? |
|---|---|---|---|---|
| Terms/Privacy pages exist, substantive | System | ✅ | Real, specific content — not placeholder | No |
| Terms/Privacy legally reviewed | 👤 Human (lawyer) | 👤 | Both pages carry their own visible disclaimer: "preliminary version under legal review" | **Yes — before commercial launch** |
| Refund policy documented | 👤 Human | ⛔ | No refund policy section exists anywhere | Yes, if Paddle multi-tier billing goes live |
| Company legal name/registration/address | 👤 Human | ⛔ | Not present anywhere on the site | Yes — before commercial launch |

## Support

| Check | Owner | Status | Evidence | Blocking? |
|---|---|---|---|---|
| Support contact channel real and monitored | 👤 Human | ⛔ | `support@rasid.app` is explicitly flagged in-code as a placeholder — no real monitored inbox exists | **Yes — blocker per the user's own Part I instruction** |

## SEO

| Check | Owner | Status | Evidence | Blocking? |
|---|---|---|---|---|
| robots.txt correct | System | ✅ | Live-verified: public pages allowed, all app/auth/admin routes disallowed | No |
| sitemap.xml correct | System | ✅ | Live-verified: 6 public URLs, correct priorities | No |
| Canonical URLs on marketing pages | System | ✅ | Verified on all 6 marketing pages | No |
| Google Search Console | 👤 Human (Google account access) | 👤 | Not performed — requires the Google account that will own the property | No for soft launch; yes before broad public marketing |

## Monitoring

| Check | Owner | Status | Evidence | Blocking? |
|---|---|---|---|---|
| Structured logging + correlation IDs | System | ✅ | pino-based, `X-Request-Id` propagated, redaction enforced | No |
| Global exception handling (API) | System | ✅ | `AllExceptionsFilter` — never leaks stack traces | No |
| Error tracking (Sentry) live | 👤 Human | ⚠️ | Not wired — no-op stubs only | Non-blocking for soft launch |
| Outbox backlog / DEAD-event visibility | 👤 Human | ⚠️ | No admin endpoint or metric — only a worker log line and manual SQL | Non-blocking for soft launch, recommend before scale |
| External uptime monitor | 👤 Human | 👤 | Not present — Railway/Vercel's own dashboards are the only signal today | Recommend before broad public launch |

## Commercial

| Check | Owner | Status | Evidence | Blocking? |
|---|---|---|---|---|
| Platform Admin shows real, truthful numbers | System | ✅ | Confirmed live in Phase 12: 16 users/16 workspaces/subscriptions-by-state, no fabricated MRR/revenue figure shown | No |
| Multi-tier billing gap resolved | 👤 Human decision | ⛔ | See Billing section above — same finding | **Yes** |
