/**
 * packages/ui — infrastructure-level placeholder only.
 *
 * The future approved design system (component library, tokens, branding)
 * is deliberately NOT implemented in Phase 0: no design references/tokens
 * exist in the repository yet. This package only reserves the workspace
 * package boundary so later phases can build the real design system here
 * without restructuring.
 */
export const UI_PACKAGE_PLACEHOLDER = true;
