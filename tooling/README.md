# tooling

Reserved for internal developer tooling (codegen scripts, custom lint rules, repo
maintenance scripts) that does not belong inside a publishable package.

Phase 0 keeps this directory as a structural placeholder only.
